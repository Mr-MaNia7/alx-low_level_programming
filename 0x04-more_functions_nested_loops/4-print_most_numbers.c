#include "main.h"

/**
 * print_most_numbers - prints most numbers
 *
 * Return: numbers except 2 and 4
 */
void print_most_numbers(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		if (c == 50 || c == 52)
			continue;
		_putchar(i);
	}
	_putchar('\n');
}
