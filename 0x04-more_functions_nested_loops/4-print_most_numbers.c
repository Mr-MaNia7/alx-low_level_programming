#include "main.h"

/**
 * print_most_numbers - print numbers
 *
 * Return: numbers
 */
void print_most_numbers(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		if (c == 52 || c == 50)
			continue;
		_putchar(i);
	}
	_putchar('\n');
}
